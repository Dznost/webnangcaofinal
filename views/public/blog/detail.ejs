<section class="blog-detail">
    <div class="container">
        <!-- Simplified blog detail structure to match event detail layout -->
        <div class="event-detail-content">
            <div class="event-image">
                <img src="<%= blog.image || 'https://images.unsplash.com/photo-1495195134817-aeb325a55b65?w=500&h=400&fit=crop' %>" alt="<%= blog.title %>" onerror="this.src='https://images.unsplash.com/photo-1495195134817-aeb325a55b65?w=500&h=400&fit=crop'">
            </div>
            <div class="event-info">
                <h1><%= blog.title %></h1>
                <p><strong>✍️ Tác giả:</strong> <%= blog.author %></p>
                <p><strong>📅 Ngày đăng:</strong> <%= new Date(blog.createdAt).toLocaleDateString('vi-VN') %></p>
                <div class="blog-content">
                    <%- blog.content %>
                </div>
            </div>
        </div>
        <!-- </CHANGE> -->
    </div>
</section>
